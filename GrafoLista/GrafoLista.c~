#include <stdio.h>
#include <stdlib.h>

typedef struct No {
	struct No Proximo;
	int vertice;
} Node;

void inserir();

int main(void){

	int v;
	Node *vet;
	printf ("Quantos vertices?");
	scanf("%d", &v);

	vet = malloc(sizeof(Node)*v);
	

}
